// var matrix = Ti.UI.create2DMatrix();
// matrix = matrix.rotate(-90);

// // $.verticalSlider.anchorPoint = { x: 0, y: 0 };
// $.sliderContainer.transform = matrix;
// $.verticalSlider.top = "-100dp";
// $.verticalSlider.left = "-15dp";
//

function doClick(e) {
    alert($.label.text);
}

function getValue() {
    alert('Spinner 1 = ' + $.spin1.value);
}

$.index.open();
